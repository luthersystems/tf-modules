image: hashicorp/terraform:0.12.23

pipelines:
  pull-requests:
    '**':
      - parallel:
          - step:
              name: Check format
              script:
                - terraform fmt -diff=true -check=true
